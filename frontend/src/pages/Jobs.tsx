import React from 'react';
import { Box, Typography } from '@mui/material';

const Jobs = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 4 }}>
        Jobs
      </Typography>
    </Box>
  );
};

export default Jobs; 